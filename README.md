Reproduction test for multithreading build failure on wasm.

To reproduce, run ``trunk serve`` from root.

Note that simply deleting .cargo/config.toml fixes it, and ``trunk serve`` shows the spinning cube.